docker-compose up -d
echo 'running the docker- though you should probably wait about 30 seconds before running the client!'
echo "once it's up, you may access the API using: localhost:5000/users, and the GUI using: localhost:8080."